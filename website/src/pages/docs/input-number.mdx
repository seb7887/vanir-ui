import { InputNumber, Flex } from 'vanir-ui'
import { Example } from '../../components'

# InputNumber

The _InputNumber_ component is similar to the _Input_ component, but it has controls for incrementing or decrementing numeric values.

## Import

```tsx
import { InputNumber } from 'vanir-ui'
```

## Usage

<Example>
  <InputNumber onChange={() => {}} />
</Example>

```tsx
<InputNumber />
```
